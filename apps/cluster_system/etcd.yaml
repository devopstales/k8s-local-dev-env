repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

helmDefaults:
  wait: true

releases:
- name: etcd-cluster
  namespace: cluster-system
  chart: bitnami/etcd
  values:
   - etcd-values.yaml