apiVersion: v1
kind: Service
metadata:
  name: lb-test
  namespace: default
spec:
  selector:
    bad: "true"
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: LoadBalancer
