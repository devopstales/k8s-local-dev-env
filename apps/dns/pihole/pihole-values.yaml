persistentVolumeClaim:
  enabled: true

hostNetwork: true

ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: ca-issuer
  hosts:
    - host: pihole.kdev.intra
      paths:
        - /
  tls:
    - secretName: pihole-tls
      hosts:
        - pihole.kdev.intra